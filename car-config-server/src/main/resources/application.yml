
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          username:
          password:
          uri: https://github.com/CarIdentification/car-config.git
          search-paths: config
      label: master
#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8761/eureka/
server:
  port: 8760

