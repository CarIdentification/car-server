spring:
  application:
    name: zuul                  #指定了配置文件的应用名
  cloud:
    config:
      uri: http://localhost:8760/        #Config server的uri
      profile: dev            #指定的环境
      label: master                      #指定分支
server:
  port: 8762